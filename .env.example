# Pangram paid API access (never commit a populated .env)
PANGRAM_API_KEY=
PANGRAM_MODEL=pangram-4
# USD per 1,000 input words; check your current Pangram plan before paid use.
PANGRAM_PRICE_PER_1000_WORDS=0.50

# Bundestag DIP API key may be required for acquisition.
DIP_API_KEY=

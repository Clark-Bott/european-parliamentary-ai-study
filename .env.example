# Pangram paid API access (never commit a populated .env)
PANGRAM_API_KEY=
PANGRAM_MODEL=pangram-4
# USD per 1,000 words; Pangram 4 rounds each text up to a 100-word block.
PANGRAM_PRICE_PER_1000_WORDS=0.50

# Bundestag acquisition currently uses the public Open Data XML list, not DIP.
